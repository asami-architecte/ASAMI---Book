<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Architecture Studio ASAMI — Book</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css">
  <style>
    html, body {
      margin: 0;
      height: 100%;
      background: #000;
      overflow: hidden;
      font-family: sans-serif;
      color: #fff;
    }

    header {
      position: absolute;
      top: 15px;
      left: 20px;
      font-size: 0.95rem;
      letter-spacing: 0.05em;
      z-index: 100;
      opacity: 0.8;
    }

    #flipbook {
      width: 100vw;
      height: 100vh;
      position: absolute;
      top: 0;
      left: 0;
    }

    .page {
      background: #111;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 0 25px rgba(0,0,0,0.6);
    }

    canvas {
      max-width: 100%;
      height: auto;
      display: block;
    }

    footer {
      position: absolute;
      bottom: 10px;
      left: 20px;
      font-size: 0.8rem;
      opacity: 0.6;
    }
  </style>
</head>
<body>
  <header>Architecture Studio ASAMI — Portfolio</header>
  <div id="flipbook"></div>
  <footer>© Architecture Studio ASAMI</footer>

  <!-- PDF.js -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.16.105/pdf.min.js"></script>
  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <!-- Turn.js -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/turn.js/4.1.0/turn.min.js"></script>

  <script>
    const url = "book.pdf"; // ton fichier PDF
    const flipbook = document.getElementById('flipbook');

    const loadingTask = pdfjsLib.getDocument(url);
    loadingTask.promise.then(function(pdf) {
      const total = pdf.numPages;

      for (let i = 1; i <= total; i++) {
        const pageDiv = document.createElement('div');
        pageDiv.classList.add('page');
        flipbook.appendChild(pageDiv);

        pdf.getPage(i).then(function(page) {
          const viewport = page.getViewport({ scale: 1.5 });
          const canvas = document.createElement('canvas');
          const context = canvas.getContext('2d');
          canvas.width = viewport.width;
          canvas.height = viewport.height;
          page.render({ canvasContext: context, viewport: viewport });
          pageDiv.appendChild(canvas);
        });
      }

      // Donne un peu de temps pour charger les pages avant d’activer Turn.js
      setTimeout(() => {
        $('#flipbook').turn({
          width: window.innerWidth,
          height: window.innerHeight,
          autoCenter: true,
          display: 'double', // double page
          acceleration: true,
          gradients: true,
          elevation: 100
        });
      }, 3000);
    });
  </script>
</body>
</html>
